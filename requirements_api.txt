# requirements_api.txt
# Dependencies untuk AIChatScraper API Server
#
# Install dengan:
#   pip install -r requirements_api.txt
#   playwright install chromium

# ── Core scraper deps (sudah ada sebelumnya) ──────────────────────────────────
playwright>=1.44.0

# ── API Server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0       # ASGI server (includes websockets, httptools)
pydantic>=2.7.0                 # data validation (FastAPI dependency)

# ── Optional: untuk client examples ──────────────────────────────────────────
openai>=1.30.0                  # OpenAI Python SDK
requests>=2.31.0                # sync HTTP client
httpx>=0.27.0                   # async HTTP client
